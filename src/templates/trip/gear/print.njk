<!DOCTYPE html>
<title>Print Sheet</title>
<h1>Gear Checkout Sheet</h1>

<dl>
  <dt>Trip ID     <dd>{{ trip.id }}</dd>
  <dt>Title       <dd>{{ trip.title }}</dd>
  <dt>Subclub     <dd>{{ trip.club_name }}</dd>
  <dt>Trip Leader <dd>{{ trip.owner_name }}</dd>
  <dt>Start Time  <dd>{{ trip.start_time | safe }}</dd>
  <dt>End Time    <dd>{{ trip.end_time | safe }}</dd>
</dl>

<style>
@media print {
  body {
    margin: 0;
    padding: 0;
  }
}

body {
  color: black;
  background-color: white;
  width: 8in;
  margin: 0 auto;
  padding: .5in;
  background-color: white;
}

dl {
  display: grid;
  grid-template-columns: max-content 1fr;
  margin-bottom: 40px;
}

dt {
  font-weight: bold;
}

section {
  margin-bottom: 80px;
}

table {
  width: 90%;
  border-collapse: collapse;
}

th, td {
  padding: 5px;
  border: 1px solid black;
}

th {
  text-align: left;
  font-weight: normal;
  border-bottom: 3px solid black;
}

td {
  height: 2rem;
}

.item {
  width: 20%;
}

.notes {
  width: 40%;
}

.returned, .packed .quantity {
  width: fit-content;
}

</style>

<section>
  <h2>Individual Gear</h2>
  <table>
    <tr>
    <th class=item>Item
    <th class=quantity>Qty
    <th class=packed>Packed?
    <th class=notes>Notes
    <th class=returned>Returned?
    <th class=condition>Condition
    </tr>
    {% for item in individual_gear %}
    <tr>
    <td class=item>{{ item.name }}
    <td class=quantity>{{ item.quantity }}
    <td class=packed>
    <td class=notes>
    <td class=returned>
    <td class=condition>
    </tr>
    {% endfor %}
    <tr>
    <td class=item>
    <td class=quantity>
    <td class=packed>
    <td class=notes>
    <td class=returned>
    <td class=condition>
    </tr><tr>
    <td class=item>
    <td class=quantity>
    <td class=packed>
    <td class=notes>
    <td class=returned>
    <td class=condition>
    </tr>
  </table>
</section>

<section>
  <h2>Group Gear</h2>
  <table>
    <tr>
    <th class=item>Item
    <th class=quantity>Qty
    <th class=packed>Packed?
    <th class=notes>Notes
    <th class=returned>Returned?
    <th class=condition>Condition
    </tr>
    {% for item in group_gear %}
    <tr>
    <td class=item>{{ item.name }}
    <td class=quantity>{{ item.quantity }}
    <td class=packed>
    <td class=notes>
    <td class=returned>
    <td class=condition>
    </tr>
    {% endfor %}
<tr>
    <td class=item>
    <td class=quantity>
    <td class=packed>
    <td class=notes>
    <td class=returned>
    <td class=condition>
    </tr>
<tr>
    <td class=item>
    <td class=quantity>
    <td class=packed>
    <td class=notes>
    <td class=returned>
    <td class=condition>
    </tr>
  </table>
</section>
