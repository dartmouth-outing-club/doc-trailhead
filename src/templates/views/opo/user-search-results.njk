
<table class=trip-table id=search-results>
  <thead>
  <tr>
    <th>Name
    <th>Email
  </tr>
  </thead>

  <tbody>
  {% for user in users %}
  <tr>
    <td><a href="/profile/{{user.id}}">{{user.name}}</a></td>
    <td>{{user.email}}</td>
  </tr>
  {% endfor %}
  </tbody>
</table>
